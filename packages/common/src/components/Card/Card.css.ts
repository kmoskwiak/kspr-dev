import { css } from '@emotion/react';
import { theme } from '../../theme';

export const styles = css`
  border: 1px solid ${theme.two};
  box-shadow: inset 0 4px 0 ${theme.two};
  padding: 0.5rem 1rem;
  border-radius: 6px;
  background-color: ${theme.one};
`